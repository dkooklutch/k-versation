const hostEmail=process.env.HOST_NOTIFICATION_EMAIL||'thekversation@gmail.com'
export async function sendEmail(input:{to:string|string[];subject:string;html:string;replyTo?:string}){if(!process.env.RESEND_API_KEY||!process.env.RESEND_FROM_EMAIL)return{skipped:true};const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${process.env.RESEND_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.RESEND_FROM_EMAIL,to:input.to,subject:input.subject,html:input.html,reply_to:input.replyTo||hostEmail})});if(!response.ok)throw new Error('Email provider rejected the message');return response.json()}
export async function notifyHost(subject:string,detail:string,replyTo?:string){return sendEmail({to:hostEmail,subject,replyTo,html:`<div style="font-family:Arial,sans-serif;max-width:620px"><p style="letter-spacing:.12em;font-size:11px">K—VERSATION / HOST NOTICE</p><h1 style="font-weight:500">${escapeHtml(subject)}</h1><p>${escapeHtml(detail)}</p></div>`})}
function escapeHtml(value:string){return value.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]!))}
