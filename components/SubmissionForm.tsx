'use client'
import { FormEvent, useState } from 'react'
export default function SubmissionForm(){const [status,setStatus]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Sending…');const form=new FormData(e.currentTarget);const r=await fetch('/api/questions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});const d=await r.json().catch(()=>({}));setStatus(r.ok?'Your note is with K-VERSATION. Thank you.':d.error||'Your note could not be saved.');if(r.ok)e.currentTarget.reset()}return <form className="form-stack" onSubmit={submit}><label>Name (optional)<input name="name" autoComplete="name" maxLength={100}/></label><label>Email address<input name="email" type="email" required autoComplete="email"/></label><label>Organization (optional)<input name="organization" autoComplete="organization" maxLength={120}/></label><label>Type<select name="category"><option>Question</option><option>Topic recommendation</option><option>Guest recommendation</option><option>Feedback</option><option>General inquiry</option></select></label><label>Question or message<textarea name="message" required minLength={10} maxLength={4000}/></label><input className="honey" name="website" tabIndex={-1} autoComplete="off" aria-hidden="true"/><button className="button">Send to K-VERSATION</button><p className="form-status" role="status">{status}</p></form>}
