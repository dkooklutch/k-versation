'use client'
import {FormEvent,useState} from 'react'
export default function JoinForm(){const[status,setStatus]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Joining…');const f=new FormData(e.currentTarget);const r=await fetch('/api/subscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json().catch(()=>({}));setStatus(r.ok?'You are part of the conversation.':d.error||'We could not save your information.');if(r.ok)e.currentTarget.reset()}return <form className="form-stack" onSubmit={submit}><label>Email address<input name="email" type="email" required autoComplete="email"/></label><label>Name (optional)<input name="name" autoComplete="name" maxLength={100}/></label><label>Age range (optional)<select name="ageRange"><option value="">Prefer not to say</option><option>Under 18</option><option>18–24</option><option>25–34</option><option>35–44</option><option>45–54</option><option>55+</option></select></label><label>Gender (optional)<input name="gender" maxLength={50}/></label><label>Country (optional)<input name="country" autoComplete="country-name" maxLength={100}/></label><label className="consent"><input type="checkbox" name="consent" value="true" required/> I agree to receive new conversation and paper announcements, milestones, and occasional editorial updates.</label><input className="honey" name="website" tabIndex={-1} autoComplete="off" aria-hidden="true"/><button className="button">Join K-VERSATION</button><p className="form-status" role="status">{status}</p></form>}
