'use client'
import{useEffect}from'react'
export default function ContentAnalytics({contentId,contentType}:{contentId:string;contentType:'conversation'|'paper'}){useEffect(()=>{if(contentType==='conversation')return;let complete=false;const send=()=>{const sessionId=sessionStorage.getItem('kv-analytics-session')||crypto.randomUUID();sessionStorage.setItem('kv-analytics-session',sessionId);fetch('/api/analytics',{method:'POST',cache:'no-store',keepalive:true,headers:{'Content-Type':'application/json'},body:JSON.stringify({eventType:'completion',contentId,contentType,path:location.pathname,sessionId})}).catch(()=>{})};const scroll=()=>{if(!complete&&scrollY+innerHeight>=document.documentElement.scrollHeight*.94){complete=true;send()}};addEventListener('scroll',scroll,{passive:true});scroll();return()=>removeEventListener('scroll',scroll)},[contentId,contentType]);return <span hidden data-content-analytics={contentType} data-content-id={contentId}/>}
