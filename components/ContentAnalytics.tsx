'use client';import{useEffect}from'react';export default function ContentAnalytics({contentId,contentType}:{contentId:string;contentType:'conversation'|'paper'}){useEffect(()=>{const sessionId=sessionStorage.getItem('kv-analytics-session')||crypto.randomUUID();sessionStorage.setItem('kv-analytics-session',sessionId);const send=(eventType:string)=>navigator.sendBeacon?.('/api/analytics',JSON.stringify({eventType,contentId,contentType,path:location.pathname,sessionId}));send('open');if(contentType==='conversation')return;let complete=false;const scroll=()=>{if(!complete&&scrollY+innerHeight>=document.documentElement.scrollHeight*.94){complete=true;send('completion')}};addEventListener('scroll',scroll,{passive:true});scroll();return()=>removeEventListener('scroll',scroll)},[contentId,contentType]);return null}
