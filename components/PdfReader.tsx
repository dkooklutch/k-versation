export default function PdfReader({url,title,downloadEnabled=false}:{url:string;title:string;downloadEnabled?:boolean}){return <section className="pdf-reader"><header><div className="eyebrow">Original paper / PDF</div>{downloadEnabled&&<a className="text-link" href={url} download target="_blank" rel="noreferrer">Download PDF <span>↓</span></a>}</header><object data={url} type="application/pdf" aria-label={`${title} PDF`}><p>Your browser cannot display this PDF. <a href={url} target="_blank" rel="noreferrer">Open the paper in a new tab.</a></p></object></section>}
