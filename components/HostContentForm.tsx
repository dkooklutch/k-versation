'use client';import{FormEvent,useState}from'react';export default function HostContentForm(){const[status,setStatus]=useState('');const[type,setType]=useState('conversation');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Saving draft…');const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/host/content',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const d=await r.json().catch(()=>({}));setStatus(r.ok?'Draft saved. It is not public until you publish it.':d.error||'Draft could not be saved.')}return <form className="host-form" onSubmit={submit}><label>Content type<select name="type" value={type} onChange={e=>setType(e.target.value)}><option value="conversation">Conversation</option><option value="paper">Paper</option></select></label><label>Title<input name="title" required maxLength={180}/></label><label>Slug<input name="slug" required pattern="[a-z0-9-]+"/></label>{type==='conversation'?<><label>Guest name<input name="guestName" required/></label><label>Category<input name="category"/></label><label>Video source URL<input name="mediaUrl" type="url"/></label></>:<><label>Subtitle<input name="subtitle"/></label><label>Topic<input name="topic"/></label><label>PDF URL<input name="pdfUrl" type="url"/></label><label>Audio URL<input name="audioUrl" type="url"/></label></>}<label>Description / body<textarea name="description" required/></label><label>Status<select name="status"><option>draft</option><option>scheduled</option><option>published</option><option>unpublished</option><option>archived</option></select></label><label>Publish or schedule date<input name="publishedAt" type="datetime-local"/></label><label>Cover asset URL<input name="imageUrl" placeholder="/editorial/bridge.jpg"/></label><div><button className="button">Save</button> <a className="button secondary" href="/" target="_blank">Preview site</a></div><p role="status">{status}</p></form>}
