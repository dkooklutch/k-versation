'use client';import{FormEvent,useState}from'react';export default function UnsubscribeForm(){const[status,setStatus]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const email=new FormData(e.currentTarget).get('email');const r=await fetch('/api/unsubscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});setStatus(r.ok?'You have been unsubscribed.':'We could not process that request.')}return <form className="form-stack" onSubmit={submit}><label>Email address<input type="email" name="email" required/></label><button className="button">Unsubscribe</button><p role="status">{status}</p></form>}
