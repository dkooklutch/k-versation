export default function HostDataPage({section,description,actions}:{section:string;description:string;actions:string[]}){return <><header className="host-head"><div><small>Manage / {section}</small><h1>{section}</h1><p>{description}</p></div></header><section className="host-panel"><div><small>Development state</small><h2>No verified records yet</h2><p>Records appear here as the connected Supabase project receives production data.</p></div><div>{actions.map(a=><button className="button secondary" key={a}>{a}</button>)}</div></section></>}
