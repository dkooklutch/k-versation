import SiteSettingsEditor from'@/components/SiteSettingsEditor';import{getSiteSettings}from'@/lib/site-settings';export const dynamic='force-dynamic';export default async function Page(){const settings=await getSiteSettings();return <><header className="host-head"><div><small>Edit / Homepage and site</small><h1>Homepage<br/>and settings</h1><p>Change the public words and contact details without touching code.</p></div></header><SiteSettingsEditor initial={settings}/></>}
