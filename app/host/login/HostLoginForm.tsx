'use client';import{FormEvent,useState}from'react';import{useRouter}from'next/navigation';export default function HostLoginForm(){const[status,setStatus]=useState('');const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Checking…');const password=new FormData(e.currentTarget).get('password');const r=await fetch('/api/host/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const d=await r.json();if(r.ok){router.push('/host/dashboard');router.refresh()}else setStatus(d.error)}return <form className="form-stack" onSubmit={submit}><label>Password<input type="password" name="password" required autoComplete="current-password"/></label><button className="button">Enter dashboard</button><p role="status">{status}</p></form>}
