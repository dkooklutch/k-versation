import{NextRequest,NextResponse}from'next/server';import{hostAuthorized}from'@/lib/host-api';import{cleanText,hasDatabase}from'@/lib/security';import{createAdminClient}from'@/lib/supabase/admin';
const statuses=new Set(['draft','scheduled','published','unpublished','archived']);
function videoSource(url:string,requested:string){if(requested==='upload')return'upload';if(/drive\.google\.com/i.test(url))return'drive';if(/youtu(?:\.be|be\.com)/i.test(url))return'youtube';if(/vimeo\.com/i.test(url))return'vimeo';return'supabase'}
export async function POST(req:NextRequest){if(!await hostAuthorized())return NextResponse.json({error:'Unauthorized'},{status:401});try{const b=await req.json();const type=b.type==='paper'?'paper':'conversation',title=cleanText(b.title,180),slug=cleanText(b.slug,180),description=cleanText(b.description,20000),status=statuses.has(b.status)?b.status:'draft',publishedAt=b.publishedAt||(status==='published'?new Date().toISOString():null);if(!title||!slug||!description||!/^[a-z0-9-]+$/.test(slug))return NextResponse.json({error:'Title, valid slug, and content are required.'},{status:400});const db=hasDatabase()?createAdminClient():null;let row:Record<string,unknown>;if(type==='conversation'){const mediaUrl=cleanText(b.mediaUrl,1200);row={title,slug,guest_name:cleanText(b.guestName,160),category:cleanText(b.category,100)||null,description,status,published_at:publishedAt,video_url:mediaUrl||null,media_source:videoSource(mediaUrl,b.mediaSource),thumbnail_url:cleanText(b.imageUrl,1000)||null};if(!row.guest_name||!mediaUrl||!/^https:\/\//i.test(mediaUrl))return NextResponse.json({error:'Guest name and a valid uploaded or linked video are required.'},{status:400});if(db){const{data,error}=await db.from('conversations').insert(row).select().single();if(error)throw error;if(data)row=data}}else{const pdfUrl=cleanText(b.pdfUrl,1200);if(!pdfUrl||!/^https:\/\//i.test(pdfUrl))return NextResponse.json({error:'Upload a PDF before saving the paper.'},{status:400});row={title,slug,subtitle:cleanText(b.subtitle,300)||null,topic:cleanText(b.topic,100)||null,excerpt:description.slice(0,300),body:[{text:description}],status,published_at:publishedAt,pdf_url:pdfUrl,pdf_download_enabled:b.pdfDownloadEnabled==='true',audio_url:cleanText(b.audioUrl,1000)||null,cover_url:cleanText(b.imageUrl,1000)||null};if(db){const{data,error}=await db.from('papers').insert(row).select().single();if(error)throw error;if(data)row=data}}if(db)await db.from('audit_logs').insert({action:'content.create',entity_type:type,entity_id:String(row.id||''),new_value:row});return NextResponse.json({ok:true,item:row})}catch{return NextResponse.json({error:'Content could not be saved.'},{status:500})}}
