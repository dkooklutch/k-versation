import type{MetadataRoute}from'next';import{conversations,papers}from'@/lib/content';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';const routes=['','/conversations','/papers','/about','/questions','/join','/privacy'];return[...routes.map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:'weekly' as const,priority:url===''?1:.7})),...conversations.map(c=>({url:`${base}/conversations/${c.slug}`,lastModified:new Date(c.publishedAt),changeFrequency:'monthly' as const,priority:.8})),...papers.map(p=>({url:`${base}/papers/${p.slug}`,lastModified:new Date(p.publishedAt),changeFrequency:'monthly' as const,priority:.8}))]}
